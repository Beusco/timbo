<Window x:Class="TimboToolApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timbo Tool - Login" Height="450" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="15" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <StackPanel Margin="40">
            <TextBlock Text="TIMBO TOOL" FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Professional Phone Service" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <TextBlock Text="Username" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,5"/>
            <TextBox x:Name="UsernameBox" Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>

            <TextBlock Text="Password" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Padding="10,8" Background="#1E293B" Foreground="White" BorderBrush="#334155" Margin="0,0,0,30"/>

            <Button Content="Login" Style="{StaticResource ModernButton}" Click="LoginButton_Click"/>
            
            <TextBlock x:Name="ErrorText" Foreground="#EF4444" Margin="0,15,0,0" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</Window>
